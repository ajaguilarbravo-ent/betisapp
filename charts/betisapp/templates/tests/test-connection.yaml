apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "betisapp.fullname" . }}-test-connection"
  labels:
    {{- include "betisapp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "betisapp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
